export enum tagTypes {
  user = "user",
  admin = "admin",
  services = "services",
  bookings = "bookings",
  blogs = "services",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.admin,
  tagTypes.services,
  tagTypes.bookings,
  tagTypes.blogs,
];
